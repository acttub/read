# read.acttub.com — 상대역 리딩 (가칭)

대본을 붙여넣고 내 배역을 고르면 **다른 배역의 대사를 소리로 읽어주거나**(`/prac`), **내 대사를 가리고 암기를 맞춰보는**(`/quiz`) 혼자 연습용 도구.

> 기획 정본은 볼트의 서브프로젝트 기획 문서다. 여기엔 작업 규칙만 둔다.
> 이름은 **가칭**이다. 확정 전까지 팀 용어처럼 쓰지 않는다.
> **소유자 1인이 단독으로 정하는** 서브프로젝트다. 팀 합의를 묻지 않고 바로 고쳐도 된다.

## 화면 4개

| 경로 | 하는 일 |
|---|---|
| `/input` | 대본 붙여넣기 → 배역 추출 (배역이 안 잡히면 이름을 직접 받아 재파싱) |
| `/char` | 내 배역 고르기 · 모드 고르기 · (읽어주기일 때만) 배역별 목소리 배정 |
| `/prac` | 리딩 실행 — 내 차례엔 멈추고, 나머지를 읽는다 |
| `/quiz` | 암기 대조 — 내 대사를 가리고, 말한 것을 원문과 맞춰본다 |

`/`는 `/input`으로 보낸다. 단계를 건너뛰면(앞 단계 데이터 없음) 앞 화면으로 되돌린다.

모드는 `read.mode`(`"read"` / `"quiz"`)가 정하고 `/char`에서 고른다. **`cue.acttub.com`으로 들어오면 `quiz`가 미리 골라진다** — 도메인이 기본값만 바꾸고 막지는 않는다. 두 도메인에서 두 모드가 다 된다.

## 절대 어기지 않는 것

- **우리 서버로는 대본도 녹음도 보내지 않는다.** 사용자가 넣는 대본은 대부분 타인의 저작물이고 서버 저장은 복제에 해당한다. 브라우저 안에서만 다루고 `sessionStorage`까지가 끝이다. 우리 쪽 서버·분석·로그 어디에도 대본 본문이 남지 않는다.
- ⚠️ **그런데 브라우저 내장 음성 기능에는 예외가 있다. "아무 데도 안 나간다"고 말하면 거짓이 된다.** (2026-07-29 코드 리뷰에서 발견)
  - **`speechSynthesis`** — 기기 안에서 도는 음성(`localService === true`)을 우선한다. 원격 음성밖에 없는 기기에서는 대사가 브라우저 음성 서비스로 전달되므로 그 사실을 화면에 알린다.
  - **`SpeechRecognition`** — **구조적으로 말소리를 브라우저 벤더 서버로 보낸다. 필터로 막을 수 없다.**
    - `/prac`의 큐 단어 넘김에서는 **선택**이다. 기본 넘김 방식은 **침묵 감지**다(음량만 보므로 오디오가 나가지 않는다). 큐 단어를 고를 때 무엇이 나가는지 알린다.
    - ⚠️ **`/quiz` 음성 모드에서는 기본 경로다.** 암기 대조가 그걸로 돌아가므로 피할 수 없다. 그래서 `/quiz`에는 **마이크를 한 번도 안 쓰고 완주되는 무음 모드가 반드시 있어야 하고**, 음성 모드를 켜는 자리에서 무엇이 나가는지 알린다. 인식 결과 텍스트는 판정하고 즉시 버린다 — 어디에도 저장하지 않는다.
    - ⚠️ **인앱 브라우저(인스타·카카오)에서는 아예 동작하지 않는다.** 안드로이드 WebView는 미지원이고, **iOS WKWebView는 `webkitSpeechRecognition`을 노출만 하고 동작은 안 한다**([WebKit #239816](https://bugs.webkit.org/show_bug.cgi?id=239816)). **즉 기능 감지가 거짓말을 한다** — `'webkitSpeechRecognition' in window`로 판정하지 말고, 실제로 `start()`하고 타임아웃으로 판정한다. 유입이 인스타라 이 경로가 사실상 첫 화면이다.
  - **사용자 노출 문구는 저장만 약속한다** — "대본은 이 기기에만 저장돼요". 전송까지 없다고 쓰지 않는다.
- **전송 경로를 바꾸면 `privacy.html`을 같이 고친다.** 그 페이지의 본문이 곧 "무엇이 어디로 나가는가"다 — `/api/tts`(ElevenLabs, 국외) · `speechSynthesis`(기기 안 또는 브라우저 음성 서비스) · `SpeechRecognition`(브라우저 벤더 서버). 코드와 어긋나는 순간 거짓 고지가 된다. **기본값을 바꿀 때도 같이 본다** — 그 페이지는 `/prac`의 기본값 조합(`engine=device` + `advanceMode=silence`)이면 아무것도 안 나간다고 적고 있고, **`/quiz`는 음성 모드일 때 말소리가 나가고 무음 모드면 안 나간다**고 따로 적고 있다. `/input`·`/char` 푸터에서 링크된다.
- ⚠️ **GA4를 붙이면 `gtag('consent','default',{analytics_storage:'denied'})`를 `gtag('config',...)` 앞에 둔다.** 이 줄이 없으면 `_ga` 쿠키에 기기 식별자가 남아 `privacy.html`의 "기기를 식별하는 값을 남기지 않습니다"가 **거짓이 된다.** `client_storage:'none'`은 옛 UA 필드라 GA4에서 통하지 않는다(voice-color에서 2026-07-29에 실제 크롬으로 확인). 쿠키를 안 쓰는 대가로 재방문을 잇지 못하지만(모든 방문이 새 사용자) 이 볼트의 서브프로젝트는 그쪽을 택했다 — **성능·정확도를 이유로 되돌리지 않는다.** 되돌리려면 `privacy.html`부터 고치고 법률 자문을 받는다.
- **연기를 평가·판정하지 않는다.** 점수·등급·"잘했어요"·"자연스러워요"·강약점·교정 문구 금지. 속도·발음·성량·억양·감정을 **계측하지도, 보여주지도 않는다.** 화면 문구는 기능 설명에 한정한다.
- ⚠️ **딱 하나만 예외다 — `/quiz`의 암기 대조** (2026-08-07에 이 줄을 좁혔다. 그전에는 "사용자 음성을 분석·비교하지 않는다"였다).
  말한 것을 **글자로 바꿔 대본 텍스트와만** 맞춰본다. 연기가 아니라 글자에 대한 것이고, 화면이 스스로 그 범위를 밝힌다("여기까지가 글자예요"). **이 예외를 근거로 다른 것을 열지 않는다.** 같이 지키는 것:
  - **점수·퍼센트·개수·등급을 표시하지 않는다.** `compare()`가 일치 비율을 반환하지 않는 것도 그래서다 — 반환값에 다시 넣지 마라
  - "맞음/틀림"이 아니라 `넘어가기` / `다시`. 모아둔 목록 이름은 **"아직 안 나온 줄"**, "정답 보기"가 아니라 **"원문 보기"**
  - **같은 줄이 두 번 미달하면 안내 없이 통과시킨다.** 편의 기능이 아니라 가드레일 장치다 — 음성인식이 사투리·억양을 오인식해 **특정 화자만 계속 막히는 것**을 구조로 막는다. 배우가 누르는 "맞게 말했는데" 버튼 하나에 기대지 않는다(그러면 기본 결과가 "다시"이고 부담을 그 특성 쪽에 지운다)
  - **"발음은 안 봐요" 같은 부정형을 쓰지 않는다.** 없다고 말하면 그 평가 프레임을 먼저 켠다 — 무엇을 하는지로만 쓴다 (볼트 `AGENTS.md`, 2026-08-06 채택)
- **마이크는 넘김 감지에만 쓰고 녹음을 남기지 않는다.** 일시정지하면 마이크 트랙을 실제로 멈춘다 — 화면은 멈췄는데 마이크가 켜진 채로 두지 않는다.

근거: 볼트 `AGENTS.md`의 제품 가드레일.

## 스택

- **Tailwind v4** — 토큰은 `src/brand/tailwind-starter.css`의 `@theme`. **직접 고치지 않는다.** 값을 바꾸려면 볼트 `brand/DESIGN.md`를 고치고 `tools/brand-tokens-export.py` → `tools/subproject-sync-brand.py`를 돌린다.
- 스타일 빌드: `npm run css` (`src/input.css` → `styles.css`). 배포는 정적이다.
- 기기 대응은 `src/brand/responsive-base.css`의 3단 티어(`phone`/`tablet`/`desktop`). `.tier-root` + `.tier-shell` 골격을 쓴다.
- 뷰포트 meta 고정: `width=device-width, initial-scale=1, viewport-fit=cover`. `maximum-scale`·`user-scalable=no` 금지.
- 높이는 `100vh` 대신 `100svh`(=`--app-min-h`). **가로 넘침을 `overflow-x: hidden`으로 덮지 않는다.**

## 폰트

UI는 Pretendard 서브셋(`build-fonts.py`), **대본 본문만 시스템 한글 글꼴**(`--font-script`)로 떨어뜨린다. 사용자가 넣는 대본은 글자가 임의라 서브셋으로 감당할 수 없어서다. 다른 서브프로젝트처럼 전부 서브셋하면 대본 절반이 깨진 글꼴로 렌더된다.

## 디자인

`design/input.pen`에 4화면 시안이 있다(파일명은 input이지만 4화면이 다 들어 있다). **캔버스는 시안까지고 구현 기준이 아니다** — pen export를 코드로 쓰지 않는다. 캔버스가 Pretendard를 렌더하지 못해 시안만 Noto Sans KR로 보이지만, `font-main` 변수는 정본대로 Pretendard다.

## 알려진 미해결 (2026-07-29 코드 리뷰에서 나왔고 사유를 달아 보류한 것)

고칠 때 다시 판단하라고 남긴다. **셋 다 기본 사용 경로가 아니거나 재현 경로를 못 찾은 것들이다.**

- **`recognition.stop()` 완료를 확인하지 않고 재시작** (`app/prac.js`) — 일부 브라우저가 `InvalidStateError`를 던질 수 있다. 실제 위험은 맞다고 본다. 제대로 고치려면 `stopCueRecognition()`을 Promise로 바꾸고 호출부(`showMyTurn`·`resumeReading` 등)를 전부 비동기로 돌려야 해서 손이 크다. **큐 단어는 기본 넘김 방식이 아니라**(기본은 침묵 감지) 노출이 적어 미뤘다.
- **버튼 연속 클릭 레이스** — 핸들러가 전부 동기 실행이고 비동기로 갈라지는 지점은 마이크 획득뿐인데 그건 Promise 재사용으로 막았다. 재현 경로를 못 찾아 보류.
- **`/api/tts`에 요청 빈도 제한이 없다.** 요청당 글자·줄 수 상한만 있어서, 반복 호출로 과금을 늘리는 걸 막지 못한다. 유입이 커지거나 비용이 눈에 띄면 그때 IP·세션 단위 제한을 건다.
- **일시정지 직후 발화가 끝나면 다음 줄이 조용히 큐에 남는다는 지적** — `pendingSpeechAdvance` 분기가 이어하기에서 정확히 그 경우를 처리하는 것으로 보여 코드 추적으로는 재현하지 못했다. 실제로 겪으면 재현 스텝을 남겨라.

## 배포

- GitHub가 정본이고 **`git push origin main`이 곧 배포다.** 로컬에서 `vercel --prod`를 치지 않는다.
- **푸시는 소유자 승인 후.** 커밋까지만 하고 멈춘다.
- 커밋 전 볼트의 기기 매트릭스 도구를 돌린다 (20개 프리셋, 문제 있으면 종료코드 1).
- `자연스러운 음성`을 쓰려면 배포 환경에 `ELEVENLABS_API_KEY`가 있어야 한다. 없으면 `/api/tts`가 전부 `null`에 `reason:"no_key"`를 돌려주고 프런트가 기기 음성으로 넘어간다 — **조용히 폴백되므로 배포 후 한 번은 실제로 확인한다.**
- ⚠️ **공개 저장소다.** 비밀값·실명·비공개 볼트 경로를 커밋하지 않는다.
