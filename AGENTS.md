# read.acttub.com — 상대역 리딩 (가칭)

> ## ⚠️ 이 브랜치(`feat/next-merge`)는 스택 교체 중이다 — 아래 본문은 아직 `main`(정적 사이트) 기준이다
>
> **소유자 결정(2026-08-26):** read.acttub.com 을 `ryujisung/read_tts`(Next 16 · React 19 · Tailwind 4) 기준으로 옮기고,
> 현행 정적 사이트의 기능을 그 위로 이관한다. 볼트 `AGENTS.md` 의 "기존 서브프로젝트 프레임워크 마이그레이션 금지"에 대한 명시적 예외다.
>
> ### 지금 상태
> - 루트 = read_tts 트리(Next 앱). 정적 사이트 원본은 `legacy/` 에 그대로 있다.
>   **이관 검증의 유일한 근거이므로 끝나기 전에 지우지 않는다** — `legacy/test/` 만 2,400줄이다.
> - 이 브랜치는 **측정용**이다. 지금 `main` 에 머지하면 아래 "아직 이관 안 된 것" 이 전부 사라진다.
>
> ### 이 브랜치에서 바꾼 것
> 1. **COEP `credentialless` → `require-corp`**(`next.config.ts`) — **Safari 는 credentialless 를 지원하지 않는다.**
>    그대로 두면 아이폰에서 교차 출처 격리가 서지 않아 wasm 스레드가 1개로 떨어진다.
>    우리가 쓰는 교차 출처 리소스는 전부 CORP/CORS 조건을 만족하는 것을 헤더로 확인했다(2026-08-26):
>    jsdelivr · googletagmanager · fonts.gstatic 은 `cross-origin-resource-policy: cross-origin`,
>    HuggingFace CDN 은 CORP 는 없지만 `fetch()` 가 기본 CORS 모드라 통과한다.
>    ⚠️ **새 외부 리소스를 붙일 때는 이 조건을 먼저 확인한다.** 안 그러면 조용히 차단된다.
> 2. `/mobile-check` 를 **비교 도구**로 바꿨다 — 교차 출처 격리·스레드 수를 띄우고,
>    되돌리기 단계 8·6·4 의 RTF 를 한 번에 잰다. `?backend=webgpu` / `?backend=wasm` 으로 장치를 강제할 수 있다.
> 3. 가중치 두 벌의 맞바꿈을 테스트로 못박았다(`models.test.ts`).
>
> ### 실측 — 이게 이 브랜치에서 나온 제일 중요한 것
> 2026-08-26 · 맥 15코어 · wasm 4스레드 · 교차 출처 격리 켜짐 · 5.1초짜리 대사 한 줄
>
> | 경로 | 받는 용량 | 준비 | 단계 8 | 단계 6 | 단계 4 |
> |---|---:|---:|---:|---:|---:|
> | `wasm + int8` | 138MB | 13.8초 | RTF **1.52** | 1.03 | 0.73 |
> | `webgpu + fp32` | 380MB | 16.7초 | RTF **0.15** | 0.05 | 0.04 |
>
> **WebGPU 가 10배 빠르다.** wasm 은 데스크톱 15코어에서도 기본 단계로 실시간을 못 따라간다
> (RTF 1.52 = 5초 대사를 만드는 데 7.7초). 폰은 이보다 느리다.
>
> ⇒ **"용량을 아끼려고 wasm 을 쓴다"는 성립하지 않는다.** 아끼는 게 아니라 못 쓰게 된다.
> 한때 `FORCED_BACKEND = "wasm"` 으로 묶었다가 이 숫자를 보고 되돌렸다.
> 남은 문제는 백엔드 선택이 아니라 **380MB 를 누구에게 언제 요구하느냐**이고, 그건 제품 결정이다.
>
> ### 다음에 할 일 — 같은 표를 폰에서 채운다
> `/mobile-check` 와 `/mobile-check?backend=wasm` 을 **HTTPS 배포본**에서 아이폰 Safari · 인스타 인앱으로 연다.
> ⚠️ `http://LAN-IP` 로 재면 secure context 가 아니라 SharedArrayBuffer 자체가 없어 **항상 1스레드 숫자**가 나온다.
> ⚠️ 프리뷰 배포에는 Vercel 로그인이 걸려 있다(`ssoProtection: all_except_custom_domains`).
>    인스타 인앱에서는 로그인이 어려우므로 대시보드에서 **Protection Bypass** 를 켜거나 프리뷰 보호를 잠시 내려야 한다.
>
> 폰 숫자가 나오면 갈린다:
> - **WebGPU 가 있고 RTF 가 넉넉하다** → 온디바이스 음성은 살아있다. 남은 건 380MB 를 첫 방문자에게 요구하지 않는 구조뿐이다.
> - **WebGPU 가 없다**(인앱 웹뷰가 여기일 가능성이 높다) → 그 기기에서 온디바이스는 답이 아니다.
>   기기 음성이나 유료 음성으로 두고, 아예 권하지 않는 편이 낫다.
>
> 미결: WebGPU 가 없는 기기에 온디바이스 음성을 **아예 안 보이게 할지**. 지금은 보이고, 받으면 느리다.
>
> ### 이관 상태 (2026-08-26)
> | 무엇 | 상태 |
> |---|---|
> | 대본 파서 실사용 수정 | ✅ 합쳤다. `parse.legacy.test.ts` 30건 + `parse.test.ts` 30건이 같이 통과한다 |
> | AI 배역 판별 | ✅ 기본 경로로 이었다. 실패·`no_key` 면 기기 안 결과로 조용히 폴백 |
> | 말하기 대조 주 엔진 | ✅ `/api/transcribe` 가 주 엔진, 브라우저 음성인식은 폴백, 그 다음 무음 |
> | 유입 계측 | ✅ GA4(consent denied 선행) + 시트 `visit`·CTA. `acttub.com` 호스트에서만 켜진다 |
> | 진입 URL | ✅ `/quiz`·`/prac` 이 모드를 정하고 `/input`·`/char`·`/script` 는 `/` 로 보낸다 |
> | 전송 고지 `/privacy` | ✅ 다시 썼다. 모델 내려받기까지 적혀 있다 |
> | 바로 쓰는 음성(ElevenLabs) | ✅ 이었다. 기본값 아님, 시작 전 일괄 준비, 실패한 줄만 기기 음성 |
> | 배역별 음성 선택 UI | ✅ 배역 화면에서 고르고 미리 들을 수 있다. 기기 음성이 둘 이상일 때만 뜬다 |
>
> ⚠️ **문안·고지·판정은 Codex 에 넘기지 않는다**(볼트 규칙). `/privacy` 는 전송 경로가 바뀔 때마다
> 사람이 다시 읽고 고친다. 실제로 두 번 다 손이 갔다 — Codex 가 쓴 고지는 모델 내려받기를 빠뜨렸고,
> 음성 고르는 화면은 그 엔진을 "유료 음성"이라 부르고 있었다(사용자가 알 일이 아니다).
>
> 이관 통과 기준은 눈이 아니라 **`legacy/test/` 가 새 코드에서 통과하느냐**다.
> 특히 파서는 두 구현이 서로 다르므로, 옛 테스트를 새 파서에 걸어 보고 판단한다.
>
> ### 파서는 합쳐졌다 (2026-08-26) — 남은 이음매 2개
> `main` 의 파서 테스트 30건을 `src/lib/script/parse.legacy.test.ts` 로 심었고, 지금은 112건 전부 통과한다.
> 형식 분해는 `main` 쪽(탭·정렬 공백·여러 줄 잇기·등장인물 구간 제거), 배역 판별은 지금 쪽
> (대괄호·제목·힌트/제외·쪽 표시·조사 필터)을 쓴다. 합치면서 남은 자리 둘:
>
> 1. **콜론 대본에서 한 줄만 말하는 단역이 지문으로 내려간다.** `main` 은 배역으로 잡는다.
>    반복 배역이 둘 이상이면 1회성 이름을 메타데이터로 보는 규칙 때문이고, 탭 대본에서는 보존된다.
>    지문은 리허설에서 읽히지 않으므로 **그 대사는 통째로 사라진다.**
>    → AI 배역 판별(`/api/parse-roles`)과 배역 편집 UI 가 이관되면 덮인다. 그때 실제로 덮이는지 확인한다.
> 2. **여러 줄 대사를 잇는 구분자가 길이 임계값(첫 조각 10자)으로 갈린다.** 두 테스트가 서로 다른 것을
>    요구해서(지금 쪽은 공백, `main` 쪽은 줄바꿈) 봉합한 자리다. 어느 계약을 쓸지 정하면 임계값은 없앤다.
>
> ### read_tts 에서 넘어온 것 중 `main` 에 없던 것
> 브라우저 안에서 도는 Supertonic 음성 · hwp·docx 대본 · 한국어 음성 품질 정렬(윈도우에서 늘 최악을 고르던 것 수정) ·
> 합성 진폭 안전장치 · 리허설 상태머신과 그 테스트 · `/mobile-check`.


대본을 붙여넣고 내 배역을 고르면 **다른 배역의 대사를 소리로 읽어주거나**(`/prac`), **내 대사를 가리고 암기를 맞춰보는**(`/quiz`) 혼자 연습용 도구.

> 기획 정본은 볼트의 서브프로젝트 기획 문서다. 여기엔 작업 규칙만 둔다.
> 이름은 **가칭**이다. 확정 전까지 팀 용어처럼 쓰지 않는다.
> **소유자 1인이 단독으로 정하는** 서브프로젝트다. 팀 합의를 묻지 않고 바로 고쳐도 된다.

## 준비·연습 화면 5개

| 경로 | 하는 일 |
|---|---|
| `/input` | 대본 붙여넣기 → 배역 추출 (배역이 안 잡히면 이름을 직접 받아 재파싱) |
| `/script` | 파싱된 대본 전체를 배역명과 대사로 확인 |
| `/char` | 내 배역 고르기 · 모드 고르기 · (읽어주기일 때만) 배역별 목소리 배정 |
| `/prac` | 리딩 실행 — 내 차례엔 멈추고, 나머지를 읽는다 |
| `/quiz` | 암기 대조 — 내 대사를 가리고, 말한 것을 원문과 맞춰본다 |

`/`는 랜딩이다. 준비 플로우는 `/input` → `/script` → `/char` 순서이며,
단계를 건너뛰면(앞 단계 데이터 없음) 앞 화면으로 되돌린다.

모드는 `read.mode`(`"read"` / `"quiz"`)가 정하고 `/char`에서 고른다. **`cue.acttub.com`으로 들어오면 `quiz`가 미리 골라진다** — 도메인이 기본값만 바꾸고 막지는 않는다. 두 도메인에서 두 모드가 다 된다.

## 절대 어기지 않는 것

- **대본 본문은 브라우저 안에서 다루고 `sessionStorage`까지만 저장한다.** 사용자가 넣는 대본은 대부분 타인의 저작물이고 서버 저장은 복제에 해당한다. 우리 쪽 서버·분석·로그 어디에도 대본 본문이 남지 않는다.
  - 예외(2026-08-24, 소유자 승인): 대본을 넣으면 휴리스틱 결과와 무관하게 **대본 앞 100,000자를 `/api/parse-roles`를 거쳐 OpenAI로 보내 배역인지 아닌지 판별한다.** AI는 배역 이름 목록만 반환하고 서버는 대본·응답을 저장하거나 기록하지 않는다. 받은 이름은 원문에서 1턴 이상 잡힐 때만 채택하고, 검증된 이름이 2개 미만이거나 요청이 실패하면 휴리스틱 결과를 유지한다. 대사 분할은 브라우저에서 기존 `parseScriptWithRoles`가 원문으로 한다.
  - 예외(2026-08-22, 소유자 승인): **`/quiz` 음성 모드의 말소리 녹음은 `/api/transcribe`를 거쳐 OpenAI로 간다** — 글자 변환에만 쓰고 서버는 녹음도 변환 텍스트도 저장·기록하지 않는다. 대본 본문은 이 음성 변환 경로로 보내지 않는다.
- ⚠️ **그런데 브라우저 내장 음성 기능에는 예외가 있다. "아무 데도 안 나간다"고 말하면 거짓이 된다.** (2026-07-29 코드 리뷰에서 발견)
  - **`speechSynthesis`** — 기기 안에서 도는 음성(`localService === true`)을 우선한다. 원격 음성밖에 없는 기기에서는 대사가 브라우저 음성 서비스로 전달되므로 그 사실을 화면에 알린다.
  - **`SpeechRecognition`** — **구조적으로 말소리를 브라우저 벤더 서버로 보낸다. 필터로 막을 수 없다.**
    - `/prac`의 큐 단어 넘김에서는 **선택**이다. 기본 넘김 방식은 **침묵 감지**다(음량만 보므로 오디오가 나가지 않는다). 큐 단어를 고를 때 무엇이 나가는지 알린다.
    - **`/quiz` 음성 모드에서는 폴백이다**(2026-08-22 전환). 주 엔진은 녹음 → `/api/transcribe`(OpenAI `gpt-4o-mini-transcribe`) — 대조 신뢰도가 곧 제품이라 정확도가 좋은 쪽을 기본으로 한다. 서버 실패·`no_key`면 브라우저 STT로, 그것도 없으면 무음으로. `/quiz`에는 **마이크를 한 번도 안 쓰고 완주되는 무음 모드가 반드시 있어야 하고**, 음성 모드를 켜는 자리에서 지금 엔진 기준으로 무엇이 어디로 나가는지 알린다. 인식 결과 텍스트는 판정하고 즉시 버린다 — 어디에도 저장하지 않는다.
    - ⚠️ **인앱 브라우저(인스타·카카오)에서는 아예 동작하지 않는다.** 안드로이드 WebView는 미지원이고, **iOS WKWebView는 `webkitSpeechRecognition`을 노출만 하고 동작은 안 한다**([WebKit #239816](https://bugs.webkit.org/show_bug.cgi?id=239816)). **즉 기능 감지가 거짓말을 한다** — `'webkitSpeechRecognition' in window`로 판정하지 말고, 실제로 `start()`하고 타임아웃으로 판정한다. 유입이 인스타라 이 경로가 사실상 첫 화면이다.
  - **사용자 노출 문구는 저장만 약속한다** — "대본은 이 기기에만 저장돼요". 전송까지 없다고 쓰지 않는다.
- **전송 경로를 바꾸면 `privacy.html`을 같이 고친다.** 그 페이지의 본문이 곧 "무엇이 어디로 나가는가"다 — `/api/parse-roles`(OpenAI, 국외) · `/api/tts`(ElevenLabs, 국외) · `/api/transcribe`(OpenAI, 국외) · `speechSynthesis`(기기 안 또는 브라우저 음성 서비스) · `SpeechRecognition`(브라우저 벤더 서버). 코드와 어긋나는 순간 거짓 고지가 된다. **기본값을 바꿀 때도 같이 본다** — 그 페이지는 `/prac`의 기본값 조합(`engine=device` + `advanceMode=silence`)이면 아무것도 안 나간다고 적고 있고, **`/quiz`는 음성 모드일 때 말소리가 나가고 무음 모드면 안 나간다**고 따로 적고 있다. `/input`·`/char` 푸터에서 링크된다.
- ⚠️ **GA4를 붙이면 `gtag('consent','default',{analytics_storage:'denied'})`를 `gtag('config',...)` 앞에 둔다.** 이 줄이 없으면 `_ga` 쿠키에 기기 식별자가 남아 `privacy.html`의 "기기를 식별하는 값을 남기지 않습니다"가 **거짓이 된다.** `client_storage:'none'`은 옛 UA 필드라 GA4에서 통하지 않는다(voice-color에서 2026-07-29에 실제 크롬으로 확인). 쿠키를 안 쓰는 대가로 재방문을 잇지 못하지만(모든 방문이 새 사용자) 이 볼트의 서브프로젝트는 그쪽을 택했다 — **성능·정확도를 이유로 되돌리지 않는다.** 되돌리려면 `privacy.html`부터 고치고 법률 자문을 받는다.
- **연기를 평가·판정하지 않는다.** 등급·"잘했어요"·"자연스러워요"·강약점·교정 문구 금지. 오디오 신호의 속도·성량·억양·감정을 **계측하지도, 보여주지도 않는다.** 화면 문구는 기능 설명에 한정한다.
- ⚠️ **딱 하나만 예외다 — `/quiz`의 암기 대조** (2026-08-07에 이 줄을 좁혔다. 그전에는 "사용자 음성을 분석·비교하지 않는다"였다).
  말한 것을 **글자로 바꿔 대본 텍스트와만** 맞춰본다. 연기가 아니라 글자에 대한 것이고, 화면이 스스로 그 범위를 밝힌다("여기까지가 글자예요"). **이 예외를 근거로 다른 것을 열지 않는다.** 같이 지키는 것:
  - 완주 요약 화면에서만 **대사 정확도·발음 정확도 비율**을 표시한다. 둘 다 음성→글자 변환 결과와 대본 텍스트 사이에서만 계산하고, 발음 정확도도 오디오 신호를 분석하지 않는다. 진행 판정의 `compare()`는 비율을 반환하지 않으며, 요약 계산은 별도 함수로 둔다.
  - 위 두 비율 밖의 점수·퍼센트·개수·등급은 표시하지 않는다. 값에 따른 색 판정이나 처방 문구도 붙이지 않는다.
  - "맞음/틀림"이 아니라 `넘어가기` / `다시`. 모아둔 목록 이름은 **"아직 안 나온 줄"**, "정답 보기"가 아니라 **"원문 보기"**
  - **같은 줄이 두 번 미달하면 안내 없이 통과시킨다.** 편의 기능이 아니라 가드레일 장치다 — 음성인식이 사투리·억양을 오인식해 **특정 화자만 계속 막히는 것**을 구조로 막는다. 배우가 누르는 "맞게 말했는데" 버튼 하나에 기대지 않는다(그러면 기본 결과가 "다시"이고 부담을 그 특성 쪽에 지운다)
  - **"발음은 안 봐요" 같은 부정형을 쓰지 않는다.** 없다고 말하면 그 평가 프레임을 먼저 켠다 — 무엇을 하는지로만 쓴다 (볼트 `AGENTS.md`, 2026-08-06 채택)
- **마이크는 넘김 감지와 `/quiz` 암기 대조에만 쓴다. 녹음은 대조 전송용으로만 잠깐 만들고 저장하지 않는다** — 전송 후 참조를 버리고, 정지·일시정지하면 마이크 트랙을 실제로 멈춘다. 화면은 멈췄는데 마이크가 켜진 채로 두지 않는다.

근거: 볼트 `AGENTS.md`의 제품 가드레일.

## 스택

- **Tailwind v4** — 토큰은 `src/brand/tailwind-starter.css`의 `@theme`. **직접 고치지 않는다.** 값을 바꾸려면 볼트 `brand/DESIGN.md`를 고치고 `tools/brand-tokens-export.py` → `tools/subproject-sync-brand.py`를 돌린다.
- 스타일 빌드: `npm run css` (`src/input.css` → `styles.css`). 배포는 정적이다.
- 기기 대응은 `src/brand/responsive-base.css`의 3단 티어(`phone`/`tablet`/`desktop`). `.tier-root` + `.tier-shell` 골격을 쓴다.
- 뷰포트 meta 고정: `width=device-width, initial-scale=1, viewport-fit=cover`. `maximum-scale`·`user-scalable=no` 금지.
- 높이는 `100vh` 대신 `100svh`(=`--app-min-h`). **가로 넘침을 `overflow-x: hidden`으로 덮지 않는다.**

## 폰트

UI는 Pretendard 서브셋(`build-fonts.py`), **대본 본문만 시스템 한글 글꼴**(`--font-script`)로 떨어뜨린다. 사용자가 넣는 대본은 글자가 임의라 서브셋으로 감당할 수 없어서다. 다른 서브프로젝트처럼 전부 서브셋하면 대본 절반이 깨진 글꼴로 렌더된다.

## 디자인

`design/input.pen`에 4화면 시안이 있다(파일명은 input이지만 4화면이 다 들어 있다). **캔버스는 시안까지고 구현 기준이 아니다** — pen export를 코드로 쓰지 않는다. 캔버스가 Pretendard를 렌더하지 못해 시안만 Noto Sans KR로 보이지만, `font-main` 변수는 정본대로 Pretendard다.

## 알려진 미해결 (2026-07-29 코드 리뷰에서 나왔고 사유를 달아 보류한 것)

고칠 때 다시 판단하라고 남긴다. **기본 사용 경로가 아니거나 재현 경로를 못 찾은 것들이다.**

- **`recognition.stop()` 완료를 확인하지 않고 재시작** (`app/prac.js`) — 일부 브라우저가 `InvalidStateError`를 던질 수 있다. 실제 위험은 맞다고 본다. 제대로 고치려면 `stopCueRecognition()`을 Promise로 바꾸고 호출부(`showMyTurn`·`resumeReading` 등)를 전부 비동기로 돌려야 해서 손이 크다. **큐 단어는 기본 넘김 방식이 아니라**(기본은 침묵 감지) 노출이 적어 미뤘다.
- **버튼 연속 클릭 레이스** — 핸들러가 전부 동기 실행이고 비동기로 갈라지는 지점은 마이크 획득뿐인데 그건 Promise 재사용으로 막았다. 재현 경로를 못 찾아 보류.
- **`/api/tts`·`/api/transcribe`·`/api/parse-roles`에 요청 빈도 제한이 없다.** 요청당 크기 상한만 있어서, 반복 호출로 과금을 늘리는 걸 막지 못한다. 유입이 커지거나 비용이 눈에 띄면 그때 IP·세션 단위 제한을 건다.
- **일시정지 직후 발화가 끝나면 다음 줄이 조용히 큐에 남는다는 지적** — `pendingSpeechAdvance` 분기가 이어하기에서 정확히 그 경우를 처리하는 것으로 보여 코드 추적으로는 재현하지 못했다. 실제로 겪으면 재현 스텝을 남겨라.

## 배포

- GitHub가 정본이고 **`git push origin main`이 곧 배포다.** 로컬에서 `vercel --prod`를 치지 않는다.
- **푸시는 소유자 승인 후.** 커밋까지만 하고 멈춘다.
- 커밋 전 볼트의 기기 매트릭스 도구를 돌린다 (20개 프리셋, 문제 있으면 종료코드 1).
- 말하기(암기 대조)의 주 엔진을 쓰려면 배포 환경에 `OPENAI_API_KEY`가 있어야 한다. 없으면 `/api/transcribe`가 `reason:"no_key"`를 돌려주고 프런트가 브라우저 STT로 조용히 폴백된다 — 배포 후 한 번은 실제로 확인한다.
- 배역 찾기 AI 기본 경로도 `OPENAI_API_KEY`를 쓴다. 없으면 `/api/parse-roles`가 `reason:"no_key"`를 돌려주고 프런트가 휴리스틱 결과로 조용히 폴백된다 — 배포 후 한 번은 실제로 확인한다.
- `자연스러운 음성`을 쓰려면 배포 환경에 `ELEVENLABS_API_KEY`가 있어야 한다. 없으면 `/api/tts`가 전부 `null`에 `reason:"no_key"`를 돌려주고 프런트가 기기 음성으로 넘어간다 — **조용히 폴백되므로 배포 후 한 번은 실제로 확인한다.**
- ⚠️ **공개 저장소다.** 비밀값·실명·비공개 볼트 경로를 커밋하지 않는다.

## 암기 대조 임계값 (2026-08-07 실측 — 다시 재려면 비용이 든다)

`app/match.js`의 통과선은 자모 편집거리 유사도 **0.72(느슨하게) · 0.95(글자 그대로)** 다.
실제 대사 14건에 음성인식이 흔히 내는 오류를 넣어 재본 결과, **느슨하게가 이걸 전부 흡수한다**:
띄어쓰기(`화난게`) · 어절 분절(`화난`↔`화가 난`) · 종성 오인식(`났어요`↔`나써요`) ·
사투리 어미(`났어예`) · 숫자 표기(`삼 년`↔`3년`) · 지문 제외 · 한 글자 오인식(`날`↔`널`).
그리고 완전히 다른 말과 절반만 말한 것은 제대로 막는다.

**흡수하지 못하는 것은 강한 사투리 하나다**(`어무이 제발 고만하이소`). 임계값을 더 낮춰
이걸 잡으려 하지 마라 — 그러면 절반만 말해도 통과한다. **그 자리를 받는 건 "같은 줄 2회
미달이면 자동 통과"이지 임계값이 아니다.**

## ⚠️ `styles.css` 는 디스크를 스캔한다 — 남의 미커밋 변경이 섞인다 (2026-08-07)

`npm run css` 는 `src/input.css` 의 `@source` 가 가리키는 **디스크의 파일**을 읽는다. 다른 세션이
`prac/index.html` 을 고쳐놓고 커밋하지 않은 상태에서 빌드하면 **그 버전에서 사라진 클래스가 CSS에서도
빠진다.** 그런데 배포되는 건 커밋된 마크업이라 — **라이브 화면이 조용히 깨진다.**

2026-08-07에 실제로 8개(`.gap-3` `.px-3` `.py-1` `.rounded-xl` `.mb-2` `.italic` `.font-normal`
`.leading-[1.45]`)가 빠진 채 커밋될 뻔했다. 전부 배포 중인 `/prac` 이 쓰는 것들이다.

**커밋 트리와 디스크가 다르면 커밋 트리로 빌드해서 넣는다.** 사본을 만들고 `git show HEAD:<파일>` 로
그 자리를 채운 뒤 거기서 `npx tailwindcss` 를 돌린다. 푸시 전에 확인하는 법 — 그 사본의 결과와
커밋된 `styles.css` 의 셀렉터 집합을 비교해서 **빠진 게 없어야** 한다(남는 건 무해하다).

⚠️ **남의 파일을 `git checkout` 으로 되돌려서 해결하지 마라.** 커밋 안 된 남의 작업이 사라진다.

## 데스크톱 배치는 `src/input.css`에 있다 (2026-08-07)

**화면 HTML에는 데스크톱 배치가 없다.** `/input`의 3열 타일, `/char`의 3열, `/quiz`의 가로 조작 줄과
대사 카드 무대 높이는 전부 `src/input.css`의 프로젝트 전용 미디어쿼리에서 온다. 화면을 고칠 때
HTML만 보면 왜 그렇게 배치되는지 알 수 없으니 거기를 같이 열어라.

**폰이 기본값이고 데스크톱이 덮는 구조다.** 폰 픽셀을 바꾸지 않고 데스크톱만 손보려면 그 블록만
건드리면 된다. 반대로 폰을 고치면 데스크톱이 따라 움직이니 양쪽을 다 찍어봐야 한다.

정본(`브랜드/DESIGN.md`)은 "구조가 티어별로 갈리면 컴포넌트 파일을 나눈다"고 하지만 여기서는 CSS로
했다 — 파일을 나누는 이유가 "두 쪽이 따로 낡는다"인데, CSS로 하면 마크업이 한 벌이라 그 위험이 없다.
**셸 폭 760px은 넘기지 않았다.** 680px 안에 3열도 가로 조작 줄도 들어간다.
